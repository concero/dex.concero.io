.itemContainer {
	gap: var(--sp-xxs);
}

.rowContainer {
	flex-direction: row;
	gap: var(--sp-xxs);
}

.headerContainer {
	flex-direction: row;
	justify-content: space-between;
	align-items: center;
}

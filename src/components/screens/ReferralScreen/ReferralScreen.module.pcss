.container {
	width: 100%;
	height: 100%;
	justify-content: center;
	flex-direction: row;
	gap: var(--sp-md);
}

.mainStack {
	width: 500px;
	gap: var(--sp-md);
}

.secondaryStack {
	width: 250px;
	gap: var(--sp-md);
}

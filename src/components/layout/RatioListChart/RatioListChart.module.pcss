.chartContainer {
	gap: var(--sp-sm);
}

.listItemHeaderContainer {
	flex-direction: row;
	align-items: center;
	justify-content: space-between;
}

.listItemContainer {
	gap: var(--sp-sm);
	width: 100%;
}

.listItemBar {
	background-color: var(--color-primary-600);
	height: 10px;
	border-radius: var(--st-br-sm);
}
